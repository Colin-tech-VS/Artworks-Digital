<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Artworksdigital — les salles ouvertes</title>
  <subtitle>Chaque artiste a sa galerie, à son adresse. Voici les salles ouvertes les plus récentes.</subtitle>
  <id>{{ canonical_url('/galeries.atom') }}</id>
  <link rel="self" type="application/atom+xml" href="{{ canonical_url('/galeries.atom') }}"/>
  <link rel="alternate" type="text/html" href="{{ canonical_url('/galeries') }}"/>
  <updated>{{ updated.strftime('%Y-%m-%dT%H:%M:%SZ') }}</updated>
  <author><name>Artworksdigital</name></author>
  <rights>Chaque œuvre appartient à son artiste.</rights>
{%- for artist in rooms %}
  <entry>
    <title>{{ artist.display_name }}{% if artist.discipline %} — {{ artist.discipline }}{% endif %}</title>
    <id>{{ canonical_url(url_for('public.gallery', slug=artist.slug)) }}</id>
    <link rel="alternate" type="text/html" href="{{ canonical_url(url_for('public.gallery', slug=artist.slug)) }}"/>
    <published>{{ artist.created_at.strftime('%Y-%m-%dT%H:%M:%SZ') }}</published>
    <updated>{{ (artist.updated_at or artist.created_at).strftime('%Y-%m-%dT%H:%M:%SZ') }}</updated>
    <author><name>{{ artist.display_name }}</name></author>
    {%- if artist.discipline %}
    <category term="{{ artist.discipline }}"/>
    {%- endif %}
    <summary type="text">{{ artist.seo_description }}</summary>
    <content type="html">&lt;p&gt;{{ artist.seo_description }}&lt;/p&gt;&lt;p&gt;{{ counts.get(artist.id, 0) }} œuvre{% if counts.get(artist.id, 0) != 1 %}s{% endif %} accrochée{% if counts.get(artist.id, 0) != 1 %}s{% endif %}.&lt;/p&gt;</content>
  </entry>
{%- endfor %}
</feed>
